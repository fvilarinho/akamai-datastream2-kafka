<source>
  @type http
  port 8888
  bind 0.0.0.0
  keepalive_timeout 300s
  <parse>
    @type none
  </parse>
</source>
<match ingest>
  @type copy
  <store>
     @type stdout
     output_type json
  </store>
</match>